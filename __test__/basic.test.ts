// Simple utility tests - start here, no React dependencies
describe('Basic Math', () => {
  test('addition works', () => {
    expect(1 + 1).toBe(2);
  });

  test('string concatenation works', () => {
    expect('Hello' + ' ' + 'World').toBe('Hello World');
  });
});
